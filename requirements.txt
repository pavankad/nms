# Network Management System (NMS) Requirements
# Minimal dependencies for core functionality

# Core web framework
Flask==3.1.2
flask-socketio==5.3.6

# Note: This NMS uses command-line SNMP tools to avoid Python SNMP library dependency conflicts
# 
# Required system packages:
# Ubuntu/Debian: sudo apt-get install snmp snmp-mibs-downloader
# RHEL/CentOS:   sudo dnf install net-snmp-utils
# macOS:         brew install net-snmp
